git config --global user.name "$USER - $HOSTNAME"
git config --global user.email "$USER"@"$HOSTNAME"
git config --global color.ui true

#--- mini guia ---#
# Actualizar el repo local (con la info en git)
# git pull

# Agregar archivos a la lista para ser subidor a git
# git add --all

# Ver que archivos estan listos para ser subidos a git
# git status

# Sincronizar los cambios en files con el repo LOCAL (aun no git)
#    Permitiendo agregar un comentario
# git commit -m 'Initial commit of all files to the repository'

# Subir cambios a git
# git push

